jQuery(function($){'use strict';function load(){TSAAPI.usage().done(function(r){var d=TSA.unwrap(r)||{},u=d.usage||{},c=d.credits||{};var credits=c.remaining!=null?c.remaining:u.credits_remaining,limit=c.limit!=null?c.limit:u.credits_limit;$('#tsa-usage-credits').text(credits!=null?credits:'—');$('#tsa-usage-credits-limit').text(limit!=null?'/ '+limit:'');$('#tsa-usage-generations').text(u.generations!=null?u.generations:'—');$('#tsa-usage-generation-limit').text(u.generations_limit!=null?'/ '+u.generations_limit:'');$('#tsa-usage-tokens').text(u.tokens!=null?u.tokens:'—');$('#tsa-usage-cost').text(u.average_cost!=null?u.average_cost:'—');var h=u.history||u.daily||[];if(!h.length){$('#tsa-usage-history').html('<div class="tsa-empty-state">No usage history returned.</div>');return;}$('#tsa-usage-history').html(h.map(function(x){var pct=Math.max(0,Math.min(100,Number(x.percent!=null?x.percent:x.value)||0));return '<div class="tsa-usage-bar"><span>'+TSA.esc(x.label||x.date||'')+'</span><i style="height:'+pct+'%" title="'+TSA.esc(x.value||pct)+'"></i></div>';}).join(''));}).fail(function(x){$('#tsa-usage-history').html('<div class="tsa-error-state">'+TSA.esc(TSA.error(x,'Unable to load usage.'))+'<br><button class="tsa-secondary tsa-retry-usage" type="button">Retry</button></div>');});}$('#tsa-refresh-usage').on('click',load);$(document).on('click','.tsa-retry-usage',load);load();});
